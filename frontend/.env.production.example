# Frontend production config — set this in your host's env vars (e.g. Vercel
# Project Settings → Environment Variables). Do NOT commit a real .env.
# Point it at your deployed backend's API base (backend URL + /api).
VITE_API_URL=https://your-backend.onrender.com/api
